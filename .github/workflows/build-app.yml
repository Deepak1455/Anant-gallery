name: Generate Android AAB & APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 📱 Setup Android SDK Tools
        uses: android-actions/setup-android@v3

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🔑 Generate Keystore & Auto assetlinks.json
        run: |
          # 1. Keystore बनाएं
          if [ ! -f "android.keystore" ]; then
            echo "Creating permanent signing keystore..."
            keytool -genkey -v -keystore android.keystore -alias android -keyalg RSA -keysize 2048 -validity 10000 -storepass androidpassword -keypass androidpassword -dname "CN=AnantGallery, OU=Mobile, O=Anant, L=Delhi, ST=Delhi, C=IN"
          fi

          # 2. SHA-256 फिंगरप्रिंट निकालें और assetlinks.json तैयार करें
          SHA_FINGERPRINT=$(keytool -list -v -keystore android.keystore -alias android -storepass androidpassword | grep "SHA256:" | awk '{print $2}')
          echo "CURRENT_SHA=$SHA_FINGERPRINT" >> $GITHUB_ENV

          cat <<EOF > assetlinks.json
          [
            {
              "relation": ["delegate_permission/common.handle_all_urls"],
              "target": {
                "namespace": "android_app",
                "package_name": "app.vercel.anantgallery.twa",
                "sha256_cert_fingerprints": [
                  "$SHA_FINGERPRINT"
                ]
              }
            }
          ]
          EOF

          echo "=========================================================="
          echo "🌟 NAYA ASSETLINKS.JSON TAIYAAR HAI:"
          cat assetlinks.json
          echo "=========================================================="

          # GitHub Actions Summary पेज पर कॉपी बटन के साथ दिखाएं
          echo "### 🚀 URL Bar Hatane Ke Liye Is Code Ko Copy Karke assetlinks.json Me Dalein:" >> $GITHUB_STEP_SUMMARY
          echo '```json' >> $GITHUB_STEP_SUMMARY
          cat assetlinks.json >> $GITHUB_STEP_SUMMARY
          echo '```' >> $GITHUB_STEP_SUMMARY

          # 3. twa-manifest.json तैयार करें
          cat <<EOF > twa-manifest.json
          {
            "packageId": "app.vercel.anantgallery.twa",
            "host": "anant-gallery.vercel.app",
            "name": "Anant Gallery",
            "launcherName": "Anant Gallery",
            "display": "standalone",
            "themeColor": "#000000",
            "navigationColor": "#000000",
            "backgroundColor": "#090d16",
            "enableNotifications": true,
            "startUrl": "/?source=pwa",
            "iconUrl": "https://raw.githubusercontent.com/Deepak1455/Anant-gallery/main/icon-512.png",
            "maskableIconUrl": "https://raw.githubusercontent.com/Deepak1455/Anant-gallery/main/icon-512.png",
            "appVersionName": "1.0.0",
            "appVersionCode": 1,
            "generatorApp": "bubblewrap-cli",
            "signingKey": {
              "path": "./android.keystore",
              "alias": "android"
            },
            "splashScreenFadeOutDuration": 300,
            "enableSiteSettingsShortcut": true,
            "isChromeOSOnly": false,
            "fullScopeUrl": "https://anant-gallery.vercel.app/",
            "minSdkVersion": 21,
            "orientation": "portrait",
            "fingerprints": [],
            "additionalTrustedOrigins": [],
            "features": {},
            "alphaDependencies": {
              "enabled": false
            }
          }
          EOF

      - name: 🛠️ Generate Android Project
        run: |
          npm install @bubblewrap/core
          node -e "
          const { TwaGenerator, TwaManifest } = require('@bubblewrap/core');
          async function buildProject() {
            const manifest = await TwaManifest.fromFile('./twa-manifest.json');
            const generator = new TwaGenerator();
            if (typeof generator.createTwaProject === 'function') {
              await generator.createTwaProject('./', manifest);
            } else if (typeof generator.create === 'function') {
              await generator.create('./', manifest);
            }
            console.log('🎉 Project Generated!');
          }
          buildProject().catch(e => { console.error(e); process.exit(1); });
          "

      - name: 📱 Inject Gallery Share Intent & Compile Signed APK/AAB
        env:
          BUBBLEWRAP_KEYSTORE_PASSWORD: "androidpassword"
          BUBBLEWRAP_KEY_PASSWORD: "androidpassword"
        run: |
          # 1. generatorApp रिसोर्स फिक्स
          if [ -f "app/src/main/res/values/strings.xml" ]; then
            sed -i 's|</resources>|    <string name="generatorApp">bubblewrap</string>\n</resources>|' app/src/main/res/values/strings.xml
          fi

          # 2. 🌟 Android गैलरी शेयर मेनू में Anant Gallery को जोड़ने का 100% पक्का कोड
          python3 -c "
          import re
          path = 'app/src/main/AndroidManifest.xml'
          try:
              with open(path, 'r') as f:
                  manifest = f.read()

              # generatorApp मेटाडेटा हटाएं
              manifest = re.sub(r'<meta-data[^>]*generatorApp[^>]*/>', '', manifest, flags=re.DOTALL)

              # गैलरी शेयर इंटेंट-फ़िल्टर इंजेक्ट करें
              share_intent = '''
                      <intent-filter>
                          <action android:name=\"android.intent.action.SEND\" />
                          <action android:name=\"android.intent.action.SEND_MULTIPLE\" />
                          <category android:name=\"android.intent.category.DEFAULT\" />
                          <data android:mimeType=\"image/*\" />
                      </intent-filter>
              '''
              if 'android.intent.action.SEND' not in manifest:
                  manifest = re.sub(r'(<activity[^>]*android:name=\"[^\"]*LauncherActivity\"[^>]*>)', r'\1\n' + share_intent, manifest)
                  with open(path, 'w') as f:
                      f.write(manifest)
                  print('✅ Gallery Share Intent Filter Injected Successfully!')
          except Exception as e:
              print(e)
          "

          # 3. Gradle से AAB और APK बनाएं
          chmod +x gradlew
          ./gradlew bundleRelease assembleRelease

          # 4. apksigner से APK को v2/v3 से साइन करें
          BUILD_TOOLS=$(ls -d $ANDROID_HOME/build-tools/* | sort -V | tail -n 1)
          if [ -f "app/build/outputs/apk/release/app-release-unsigned.apk" ]; then
            $BUILD_TOOLS/zipalign -v -p 4 app/build/outputs/apk/release/app-release-unsigned.apk app/build/outputs/apk/release/aligned.apk
            $BUILD_TOOLS/apksigner sign --ks android.keystore --ks-pass pass:androidpassword --key-pass pass:androidpassword --out app/build/outputs/apk/release/anant-gallery-signed.apk app/build/outputs/apk/release/aligned.apk
            echo "✅ Signed with official apksigner!"
          fi

      - name: 📦 Upload AAB, Signed APK & assetlinks.json
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Anant-Gallery-Android-Build
          path: |
            **/build/outputs/bundle/release/*.aab
            **/build/outputs/apk/release/anant-gallery-signed.apk
            assetlinks.json
            android.keystore
            **/*.aab
          retention-days: 14
