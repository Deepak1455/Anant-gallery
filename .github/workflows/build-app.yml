name: Generate Android AAB & APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: ☕ Setup Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 📱 Setup Android SDK Tools
        uses: android-actions/setup-android@v3

      - name: 📦 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: 🔑 Generate Keystore & twa-manifest
        run: |
          # 1. यदि Keystore नहीं है तो बनाएं
          if [ ! -f "android.keystore" ]; then
            echo "Creating signing keystore..."
            keytool -genkey -v -keystore android.keystore -alias android -keyalg RSA -keysize 2048 -validity 10000 -storepass androidpassword -keypass androidpassword -dname "CN=AnantGallery, OU=Mobile, O=Anant, L=Delhi, ST=Delhi, C=IN"
          fi

          # 2. twa-manifest.json तैयार करें (नाम सेट किया गया: Anant Gallery)
          cat <<EOF > twa-manifest.json
          {
            "packageId": "app.vercel.anantgallery.twa",
            "host": "anant-gallery.vercel.app",
            "name": "Anant Gallery",
            "launcherName": "Anant Gallery",
            "display": "standalone",
            "themeColor": "#000000",
            "navigationColor": "#000000",
            "backgroundColor": "#090d16",
            "enableNotifications": true,
            "startUrl": "/?source=pwa",
            "iconUrl": "https://raw.githubusercontent.com/Deepak1455/Anant-gallery/main/icon-512.png",
            "maskableIconUrl": "https://raw.githubusercontent.com/Deepak1455/Anant-gallery/main/icon-512.png",
            "appVersionName": "1.0.0",
            "appVersionCode": 1,
            "generatorApp": "bubblewrap-cli",
            "signingKey": {
              "path": "./android.keystore",
              "alias": "android"
            },
            "splashScreenFadeOutDuration": 300,
            "enableSiteSettingsShortcut": true,
            "isChromeOSOnly": false,
            "fullScopeUrl": "https://anant-gallery.vercel.app/",
            "minSdkVersion": 21,
            "orientation": "portrait",
            "fingerprints": [],
            "additionalTrustedOrigins": [],
            "features": {},
            "alphaDependencies": {
              "enabled": false
            }
          }
          EOF
          echo "✅ Keystore and twa-manifest.json ready with name Anant Gallery!"

      - name: 🛠️ Generate Android Project (Zero Prompts)
        run: |
          npm install @bubblewrap/core
          node -e "
          const { TwaGenerator, TwaManifest } = require('@bubblewrap/core');
          async function buildProject() {
            const manifest = await TwaManifest.fromFile('./twa-manifest.json');
            const generator = new TwaGenerator();
            if (typeof generator.createTwaProject === 'function') {
              await generator.createTwaProject('./', manifest);
            } else if (typeof generator.create === 'function') {
              await generator.create('./', manifest);
            }
            console.log('🎉 Android Native Project Generated Successfully!');
          }
          buildProject().catch(e => { console.error(e); process.exit(1); });
          "

      - name: 🔨 Fix Resources, Compile & Sign APK
        env:
          BUBBLEWRAP_KEYSTORE_PASSWORD: "androidpassword"
          BUBBLEWRAP_KEY_PASSWORD: "androidpassword"
        run: |
          # 1. generatorApp स्ट्रिंग को strings.xml में जोड़ें
          if [ -f "app/src/main/res/values/strings.xml" ]; then
            sed -i 's|</resources>|    <string name="generatorApp">bubblewrap</string>\n</resources>|' app/src/main/res/values/strings.xml
          fi

          # 2. AndroidManifest से meta-data साफ करें
          python3 -c "
          import re
          try:
              with open('app/src/main/AndroidManifest.xml', 'r') as f:
                  c = f.read()
              c = re.sub(r'<meta-data[^>]*generatorApp[^>]*/>', '', c, flags=re.DOTALL)
              with open('app/src/main/AndroidManifest.xml', 'w') as f:
                  f.write(c)
          except Exception as e:
              pass
          "

          # 3. Gradle से सीधे AAB और APK बनाएं
          chmod +x gradlew
          ./gradlew bundleRelease assembleRelease

          # 4. APK को साइन करें ताकि फ़ोन में बिना एरर इनस्टॉल हो सके
          if [ -f "app/build/outputs/apk/release/app-release-unsigned.apk" ]; then
            echo "Signing APK for direct phone installation..."
            jarsigner -verbose -sigalg SHA256withRSA -digestalg SHA-256 -keystore android.keystore -storepass androidpassword -keypass androidpassword app/build/outputs/apk/release/app-release-unsigned.apk android
            cp app/build/outputs/apk/release/app-release-unsigned.apk app/build/outputs/apk/release/anant-gallery-signed.apk
          fi

      - name: 📦 Upload Official AAB & Signed APK
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: Anant-Gallery-Android-Build
          path: |
            **/build/outputs/bundle/release/*.aab
            **/build/outputs/apk/release/anant-gallery-signed.apk
            **/build/outputs/apk/release/*.apk
            **/*.aab
          retention-days: 14
